name: Deploy Crop API

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image
        run: |
          docker build -t syedmawahid/crop-api:latest .

      - name: Push Docker image
        run: |
          docker push syedmawahid/crop-api:latest

      - name: Deploy on server via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            docker pull syedmawahid/crop-api:latest
            docker stop crop-api || true
            docker rm crop-api || true
            docker run -d \
              --name crop-api \
              -p 8000:8000 \
              --restart unless-stopped \
              syedmawahid/crop-api:latest
